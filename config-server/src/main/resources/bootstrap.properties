### Spring
spring.application.name = config-server

### Spring PROFILE
# "native" profile: config-server does not use git, but loads the config files from the local classpath or file system.
spring.profiles.active  = local,native

### Spring CLOUD CONFIG
## Server
#spring.cloud.config.server.encrypt.enabled = true
